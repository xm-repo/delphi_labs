program lab2;

{$APPTYPE CONSOLE}

uses
  SysUtils;

var
  count, i, number, sum: Integer;
begin
  Read(count);

  sum := 1;
  for i := 1 to count do begin
    Read(number);
    sum := sum * number;
  end;

  Halt(Sum);

end.

